"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveSitemapOptions = void 0;
const resolveSitemapOptions = (themeConfig, options) => {
    if (options === false)
        return false;
    // disable feed if `hostname` is not set and no options for feed plugin
    if (!Object.keys(options || {}).length && !themeConfig.hostname)
        return false;
    return {
        hostname: themeConfig.hostname,
        ...(options || {}),
    };
};
exports.resolveSitemapOptions = resolveSitemapOptions;
//# sourceMappingURL=sitemap.js.map